✅ SOC Access Tool – Full Progress Summary (April 3)
🔐 Environment + Access Setup
Gained CLI access to the SOC1 Workstation VM via SSH using PuTTY.
Verified access from kboller@soc1.az-eus-vws.marchex.com.
Installed and configured Xming + X11 forwarding for running GUI tools remotely:
Confirmed GUI rendering by successfully launching xclock.
Documented that GUI-based scripts (e.g., soctool.py) should be tested through PuTTY + Xming, not via Cursor RDP.
💻 Project Migration + Git Workflow
Moved entire SocTool project from local dev system to ~/socbot/SocTool on the SOC1 VM.
Cleaned up the project structure and confirmed all core scripts were present (e.g., soctool.py, access_management.py, Utils/, etc.).
Created and updated the .gitignore file:
Added system-specific and runtime log file exclusions.
Ensured secrets and user-specific paths (e.g., Logs/, .vscode/, __pycache__/) are ignored.
Successfully committed and pushed changes from the SOC1 VM to the Git repo, verifying correct Git remote and SSH access.
🛠️ GUI Script Refactoring (soctool.py)
Refactored the GUI logic to remove LDAP authentication from the frontend.
The GUI now only collects input and passes credentials to the backend.

Updated subprocess call to use:

python3 access_management.py [action] [target_user] --ldap_user [...] --ldap_pass [...]
Added error-handling output to the GUI log window to show backend failures in real time.
🧩 LDAP Authentication Fixes (access_management.py)
Migrated all LDAP auth logic into access_management.py for simplicity.
Updated bind method to use:
SIMPLE authentication
UPN-style login (e.g., kboller@marchex.com)
ldaps with port 636
Updated server to:
ldaps://ldap.oci-sjc-prd.marchex.com (OCI West Coast – supported, non-AWS endpoint)
Injected helpful debug log lines:
Shows exactly which bind user and server are being tried
Catches common issues like invalidDNSyntax and authMethodNotSupported
🧪 Testing + Issues Discovered
Ran the GUI successfully via X11 from PuTTY.
Repeated tests using various LDAP username formats:
Verified UPN logic works (e.g., appends @marchex.com if missing)
Encountered consistent invalidDNSyntax errors from the LDAP bind step.
Confirmed these errors are coming from the server, not the script itself.
Likely related to the server expecting a different format for user (e.g., full DN or domain\\user).
Will require further trial of bind formats or escalation to IT/Infra.
📝 Next Steps
Run access_management.py directly with known-working creds to isolate the error more easily outside GUI.
Test alternate bind formats:
"marchex\\kboller" (domain-based)
"cn=kboller,dc=marchex,dc=com" (distinguished name)
Verify correct bind style supported by ldap.oci-sjc-prd.marchex.com.
Add field validation to the GUI for better user feedback (optional).
Finalize mock mode toggle and RT ticket passthrough once auth is solid.

Let me know if you want this turned into a formatted ADO comment (Markdown or plaintext), or want it emailed/slacked out as a project recap.